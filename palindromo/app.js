(function () {
	'use strict'

	angular
		.module('prac1',[])
		.controller('pracCtrl',pracCtrl);


		first.palin2="";

		function pracCtrl() {
			var first = this;

			first.palindromo = prompt("dime tu palindromo");

			first.palin=function(){
				first.palindromo= palindromo.toLowerCase();
			}

			first.palin1=function(){
				first.palin=palin.replace(/\s/g, '');
			}
			
			
					
			for ( var i=palin1.length - 1; i>=0 ; i--){
			palin2=palin1+palindromo1[i];
		
	};


		}

		




})();